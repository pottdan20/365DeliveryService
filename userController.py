def checkIfUserExist(username):
    print("checking if user exist")

def createUser(username, password): # create and return the user object created
    print("creating " + username + "...")
    return 12345

def attemptLogin(username, password): #will attempt to login with current credentials and either throw an error if no user exisits
    print("attempting login")          # or will return user object
    return 12345                        